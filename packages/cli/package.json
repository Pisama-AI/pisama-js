{
  "name": "@pisama/cli",
  "version": "0.11.2",
  "description": "CLI for Pisama. `init` patches your project, `verify` round-trips a verification trace, `mcp` runs the MCP server, `analyze-atif` runs Pisama detectors on Harbor trajectories (locally via --local, or against the hosted API).",
  "keywords": [
    "ai",
    "agents",
    "observability",
    "failure-detection",
    "mcp",
    "harbor",
    "atif",
    "vercel-ai-sdk"
  ],
  "type": "module",
  "bin": {
    "pisama": "./dist/bin.js",
    "pisama-ts": "./dist/bin.js"
  },
  "files": [
    "dist",
    "CHANGELOG.md",
    "THIRD_PARTY_NOTICES.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prebuild": "pnpm --filter @pisama/detectors build",
    "build": "tsc -p tsconfig.json && esbuild src/mcp.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/mcp.js --legal-comments=inline",
    "prepack": "pnpm run build",
    "dev": "tsc -p tsconfig.json --watch",
    "pretypecheck": "pnpm --filter @pisama/detectors build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm run build",
    "test": "tsx --test test/*.test.ts",
    "pretest:coverage": "pnpm run build",
    "test:coverage": "node --import tsx --test --experimental-test-coverage --test-coverage-lines=95 --test-coverage-branches=75 --test-coverage-include=\"src/**\" --test-coverage-include=\"test/**\" test/*.test.ts"
  },
  "dependencies": {
    "@pisama/detectors": "workspace:*",
    "commander": "^15.0.0",
    "kleur": "^4.1.5",
    "nanoid": "^6.0.0",
    "open": "^11.0.0",
    "ts-morph": "^28.0.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@types/node": "^22.0.0",
    "esbuild": "0.28.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Pisama-AI/pisama-js",
    "directory": "packages/cli"
  },
  "homepage": "https://pisama.ai",
  "bugs": {
    "url": "https://github.com/Pisama-AI/pisama-js/issues"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  }
}
