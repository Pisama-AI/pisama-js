// Kept in sync with packages/sdk/package.json `version` field. Bumped manually
// on every release so the SDK can self-report its version in the
// `x-pisama-sdk-version` header for the backend `sdk_first_run` event.
export const SDK_VERSION = '0.9.0-alpha.4';
